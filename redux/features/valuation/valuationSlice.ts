import { createSlice } from "@reduxjs/toolkit";

const initialState = {};

const valuationSlice = createSlice({
  name: "valuation",
  initialState,
  reducers: {},
});

export const {} = valuationSlice.actions;

export default valuationSlice.reducer;
